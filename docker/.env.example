# =============================================================================
# OpenFrame Docker Configuration
# =============================================================================
# No configuration is required for a quick start!
# Just run: docker compose up -d
#
# All secrets (JWT, cookie, encryption) are auto-generated on first run.
# OAuth and integrations can be configured later via the Setup Wizard in the UI.
#
# For production deployments, uncomment and set the values below.
# Generate secrets with: openssl rand -hex 32
# =============================================================================

# Frontend URL (set this to your server's URL for non-localhost deployments)
# Used for CORS origins and OAuth redirect URLs
# FRONTEND_URL=http://your-server:8080

# Database credentials (defaults are used if not set)
# POSTGRES_USER=openframe
# POSTGRES_PASSWORD=openframe
# POSTGRES_DB=openframe

# Security secrets (auto-generated and persisted if not provided)
# JWT_SECRET=
# COOKIE_SECRET=
# ENCRYPTION_KEY=

# Google OAuth (https://console.cloud.google.com/)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=http://localhost:3001/api/v1/auth/oauth/google/callback

# Microsoft OAuth (https://portal.azure.com/)
# MICROSOFT_CLIENT_ID=
# MICROSOFT_CLIENT_SECRET=
# MICROSOFT_REDIRECT_URI=http://localhost:3001/api/v1/auth/oauth/microsoft/callback

# Telegram Bot (https://t.me/BotFather)
# TELEGRAM_BOT_TOKEN=
# BOT_API_KEY=

# Home Assistant (optional)
# HOME_ASSISTANT_URL=
# HOME_ASSISTANT_TOKEN=
