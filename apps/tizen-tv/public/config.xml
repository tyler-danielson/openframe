<?xml version="1.0" encoding="UTF-8"?>
<widget xmlns="http://www.w3.org/ns/widgets"
        xmlns:tizen="http://tizen.org/ns/widgets"
        id="http://openframe.local"
        version="1.0.0">
    <name>OpenFrameKiosk</name>
    <icon src="icon.png"/>
    <tizen:application id="OFrm000001.OpenFrameKiosk" package="OFrm000001" required_version="6.0"/>

    <!-- Required privileges for TV app -->
    <tizen:privilege name="http://tizen.org/privilege/tv.inputdevice"/>
    <tizen:privilege name="http://tizen.org/privilege/application.launch"/>
    <tizen:privilege name="http://tizen.org/privilege/internet"/>
    <tizen:privilege name="http://developer.samsung.com/privilege/productinfo"/>

    <!-- Allow network access to any server (needed for QR login fetch calls) -->
    <access origin="*" subdomains="true"/>

    <!-- Entry point -->
    <content src="index.html"/>

    <!-- Support all screen sizes -->
    <feature name="http://tizen.org/feature/screen.size.all"/>

    <!-- App settings -->
    <tizen:setting screen-orientation="landscape"
                   context-menu="disable"
                   background-support="enable"
                   encryption="disable"/>
</widget>
